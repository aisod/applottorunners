<?xml version="1.0" encoding="utf-8"?>
<!--
  DO NOT add the real Mapbox token here.
  This file is a placeholder only.

  The real mapbox_access_token string resource is injected at build time by
  Gradle via resValue in android/app/build.gradle, reading from the
  MAPBOX_ACCESS_TOKEN property in your local android/gradle.properties
  (which is gitignored).

  See android/gradle.properties.example for setup instructions.
-->
<resources>
    <!-- placeholder — overridden by Gradle resValue at build time -->
    <string name="mapbox_access_token_placeholder">REPLACED_AT_BUILD_TIME</string>
</resources>
